# CLPoly 核心类型使用模式扫描

为 `type-system.md §3 CLPoly 核心类型` 设计提供输入。

## 使用频次（总览）

| 类型 | 总出现 | 变量 | 参数 | 方法数 | 字段数 | 宿主数 |
|---|---|---|---|---|---|---|
| `clpoly::lex_` | 229 | 7 | 0 | 0 | 0 | 14 |
| `clpoly::ZZ` | 156 | 1 | 0 | 3 | 3 | 25 |
| `clpoly::Zp` | 77 | 0 | 0 | 3 | 3 | 22 |
| `clpoly::basic_monomial` | 53 | 5 | 0 | 2 | 2 | 16 |
| `clpoly::umonomial` | 25 | 0 | 0 | 1 | 1 | 13 |
| `clpoly::variable` | 13 | 1 | 0 | 0 | 0 | 7 |
| `clpoly::grlex_` | 12 | 0 | 0 | 0 | 0 | 1 |

## `clpoly::lex_`

- 总出现: **229**（var 7 + param 0）
- 宿主函数: **14** 个

## `clpoly::ZZ`

- 总出现: **156**（var 1 + param 0）
- 宿主函数: **25** 个

### 方法调用

| 方法 | 调用次数 |
|---|---|
| `.operator bool()` | 12 |
| `.sizeinbase()` | 3 |
| `.fdiv_ui()` | 3 |

### 字段访问

| 字段 | 访问次数 |
|---|---|
| `.operator bool` | 12 |
| `.sizeinbase` | 3 |
| `.fdiv_ui` | 3 |

### 构造方式

| 构造种类 | 次数 |
|---|---|
| `copy/move` | 4 |

## `clpoly::Zp`

- 总出现: **77**（var 0 + param 0）
- 宿主函数: **22** 个

### 方法调用

| 方法 | 调用次数 |
|---|---|
| `.prime()` | 9 |
| `.inv()` | 2 |
| `.number()` | 1 |

### 字段访问

| 字段 | 访问次数 |
|---|---|
| `.prime` | 9 |
| `.inv` | 2 |
| `.number` | 1 |

## `clpoly::basic_monomial`

- 总出现: **53**（var 5 + param 0）
- 宿主函数: **16** 个

### 方法调用

| 方法 | 调用次数 |
|---|---|
| `.begin()` | 4 |
| `.end()` | 4 |

### 字段访问

| 字段 | 访问次数 |
|---|---|
| `.begin` | 4 |
| `.end` | 4 |

### 构造方式

| 构造种类 | 次数 |
|---|---|
| `default` | 4 |

## `clpoly::umonomial`

- 总出现: **25**（var 0 + param 0）
- 宿主函数: **13** 个

### 方法调用

| 方法 | 调用次数 |
|---|---|
| `.deg()` | 16 |

### 字段访问

| 字段 | 访问次数 |
|---|---|
| `.deg` | 16 |

## `clpoly::variable`

- 总出现: **13**（var 1 + param 0）
- 宿主函数: **7** 个

## `clpoly::grlex_`

- 总出现: **12**（var 0 + param 0）
- 宿主函数: **1** 个
